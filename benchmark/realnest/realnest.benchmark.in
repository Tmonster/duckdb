# name: ${FILE_PATH}
# description: ${DESCRIPTION}
# group: [realnest]

name Q${QUERY_NUMBER_PADDED}
group realnest

require httpfs

require json

cache realnest.duckdb

load benchmark/realnest/queries/load.sql

run benchmark/realnest/queries/q${QUERY_NUMBER_PADDED}.sql